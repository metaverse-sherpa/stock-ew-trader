import React from 'react';
import Home from '../components/home'; // Import the Home component

const HomePage = () => {
  return <Home />; // Render the Home component instead of the placeholder
};

export default HomePage; 